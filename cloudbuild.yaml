steps:
  # Step 1: Install dependencies and build the NestJS app
  - name: 'node:lts'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        npm install
        npm run build --filter=backend

  # Step 2: Deploy to Cloud Run directly from source
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'saaszo-backend'
      - '--source'
      - '.' 
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'

options:
  logging: CLOUD_LOGGING_ONLY
